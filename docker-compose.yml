version: '3'
services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "5000:5000"
    restart: always
    command: python app.py
  email_forecast:
    build: .
    env_file:
      - .env
    restart: always
    command: python email_forecast.py
